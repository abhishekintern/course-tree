import Curriculum from "@/components/tree/Curriculum";

const treebar = () => {
  return (
    <div className="bg-slate-100 w-full h-screen">
      <Curriculum />
    </div>
  );
};

export default treebar;
